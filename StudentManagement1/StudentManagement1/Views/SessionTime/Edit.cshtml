@model StudentManagement1.SessionTime

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SessionTime</legend>

        @Html.HiddenFor(model => model.SessionTime_Id)
        <div class="editor-label">
            @Html.LabelFor(model => model.Session_Time)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Session_Time)
            @Html.ValidationMessageFor(model => model.Session_Time)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
